apiVersion: apps/v1
kind: Deployment
metadata:
  name: user walking stat
  labels:
    app: user walking stat
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user walking stat
  template:
    metadata:
      labels:
        app: user walking stat
    spec:
      containers:
        - name: user walking stat
          image: "username/user walking stat:latest"
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: '/actuator/health'
              port: 8080
            initialDelaySeconds: 10
            timeoutSeconds: 2
            periodSeconds: 5
            failureThreshold: 10
          livenessProbe:
            httpGet:
              path: '/actuator/health'
              port: 8080
            initialDelaySeconds: 120
            timeoutSeconds: 2
            periodSeconds: 5
            failureThreshold: 5
